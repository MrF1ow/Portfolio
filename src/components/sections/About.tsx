import { work as workConfig } from "@/config/site";

export default function About() {
  return (
    <div className="flex w-full flex-col lg:flex-row">
      <div className="w-[50%]">
        
      </div>
    </div>
  );
}
